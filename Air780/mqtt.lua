

local mqtt_host = "git.zgwit.com"
local mqtt_port = 1883
local mqtt_clienid = mobile.getImei()
local mqtt_username = ""
local mqtt_password = ""

local mqtt_client = nil

function mqtt_open()
    
end

function mqtt_close()
    
end

function mqtt_publish(topic, payload)
    
end

function mqtt_subscribe(filter, cb)
    
end
